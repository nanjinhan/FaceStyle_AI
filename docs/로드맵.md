# 로드맵 — 현재 구현 현황 · 갭 분석 · 마일스톤

> 기준: `docs/기능명세서.md` v1.0 (2026-07-18)

## 1. 지금 있는 것 (친구가 만든 backend v0.1)

FastAPI 백엔드만 존재하며 앱(프론트)은 없다. 실시간 방의 뼈대는 이미 잘 잡혀 있다.

| 영역 | 위치 | 상태 |
|---|---|---|
| 소셜 로그인(스텁) / 게스트 참여 | `routers/auth.py`, `sessions.py: join_session` | ✅ 동작 (실제 OAuth 검증은 미연동) |
| 방 생성 + 사진 업로드(1장) | `sessions.py: create_session` | ✅ |
| 초대 링크/코드, 인원 제한, 만료 | `models.EditSession` | ✅ (단, 4명/72h — 명세는 6명/24h) |
| 방장 잠금/강퇴 | `toggle_lock`, `kick_member` | ✅ |
| 서버측 얼굴 검출(보조) | `face_detection.py` | ✅ (OpenCV 있을 때만) |
| 얼굴 클레임 | `routers/faces.py` | ✅ |
| 보정 파라미터 모델(전역/얼굴별) | `collab/state.py` | ✅ LWW + 버전 |
| 실시간 동기화/프레즌스/소프트락/리액션 | `routers/ws.py`, `collab/manager.py` | ✅ |
| undo/redo | `collab/state.py` | ⚠️ 사진당 **공유 스택** — 명세는 **개인별** |
| 저장 확정 + 배포 | `sessions.py: save_photo_endpoint` | ⚠️ 방장 단독 확정 — 명세는 **전원 완료 체크** |

## 2. 갭 분석 (명세 v1.0 대비 없는 것)

### 백엔드

| # | 갭 | 명세 근거 | 작업 위치 |
|---|---|---|---|
| B1 | 유저 고유 색상 자동 배정 | 회원관리 | `User.color` 컬럼 + 가입 시 배정 |
| B2 | 닉네임 변경, 로그아웃/탈퇴(방장 앨범 위임·삭제 강제) | 회원관리 | `routers/users.py` 신규 |
| B3 | 클레임 영역 **서버측 편집 권한 강제** (현재 아무나 아무 path 편집 가능) | 실시간 방 — 편집 권한 분리 | `ws.py: _handle_message` |
| B4 | 공용 영역(global) 권한 설정 — 방장 전용/전체 허용 | 실시간 방 — 공용 영역 권한 | `EditSession` 설정 + `ws.py` 검사 |
| B5 | **개인별** undo/redo (member_id 기준 스택 분리) | 실시간 방 — 되돌리기 | `collab/state.py` |
| B6 | 기능별 리셋 (`reset_param`) | 실시간 방 — 되돌리기 | `ws.py` + `state.py` |
| B7 | 완료 확정: 전원 체크 → 자동 확정 + 편집 잠금 | 실시간 방·앨범 — 확정 | `PhotoCompletion` 모델 + WS `complete` |
| B8 | 방 사진 **다중 업로드 + 컷 선택** (현재 1장 고정) | 실시간 방 — 사진 선택 | `sessions.py` |
| B9 | 방 설정값 수정: 6명 / 24h 만료 + 만료 시 **앨범 이관** | 실시간 방 — 종료 | `config.py` + 만료 잡 |
| B10 | **앨범 전체**: CRUD, 멤버(초대/나가기/내보내기/위임), 사진 업로드, 상태 뱃지, 할 일 필터, 비동기 보정, 방장 마감, 사진 삭제 | 앨범 1~14행 | `routers/albums.py` + 모델 신규 |
| B11 | 알림: 목록 API + FCM 푸시 (업로드/완료/초대), 앨범별 on/off | 메인 — 알림, 앨범 — 업로드 알림 | `routers/notifications.py` |
| B12 | 홈 피드: 내 앨범 목록 + 최근 작업 + 할 일 개수 (무한 스크롤 페이징) | 메인페이지 | `routers/home.py` 또는 albums에 포함 |
| B13 | 실제 소셜 OAuth 검증 (카카오/Google/Apple) | 회원관리 | `auth.py` |

### 앱 (Flutter — 전부 신규)

| # | 화면/기능 | 명세 근거 |
|---|---|---|
| A1 | 로그인/가입/닉네임 설정/마이페이지 | 회원관리 |
| A2 | 홈 (앨범 카드, 최근 사진, 할 일 배너, 무한 스크롤) | 메인페이지 |
| A3 | 인앱 카메라 | 메인페이지 |
| A4 | 알림 목록 + 푸시 수신·탭 이동 | 메인페이지 |
| A5 | 실시간 방 (초대 공유, 참여자 아바타, 컷 선택) | 실시간 방 |
| A6 | 보정 에디터: 얼굴 클레임 UI, 본인/타인 영역 잠금 표시, 파라미터 슬라이더, 실시간 커서, 작업 상태 라벨, 개인 undo/redo, 기능별 리셋, 개인 저장, 완료 체크 | 실시간 방 |
| A7 | 온디바이스 얼굴검출(MLKit) + 파라미터 기반 이미지 렌더링 파이프라인 | 에디터 공통 |
| A8 | 앨범 목록/상세 (상태 뱃지, 할 일 필터, 업로드, 멤버 관리, 방장 마감) | 앨범 |
| A9 | 딥링크 (`facestyle://join`) + 미설치 시 스토어 → 설치 후 자동 입장 | 초대 공통 |
| A10 | 게스트 모드 (닉네임만으로 참여) | 회원관리 |

## 3. 마일스톤

**M0 — 프로젝트 구성 (지금)**
모노레포 구조, 문서, Flutter 스캐폴드, 백엔드 실행 확인.

**M1 — 실시간 방 E2E (백엔드 보강 + 에디터 최소 기능)**
B3·B5·B7·B8·B9 + A5·A6(슬라이더 3~4개만)·A7(전역 보정만). 두 기기에서 같은 사진을 실시간으로 같이 보정하고 전원 완료 확정까지. **여기가 앱의 코어 데모.**

> M1 진행 현황 (2026-07-20)
>
> | 단계 | 항목 | 상태 |
> |---|---|---|
> | 1 | **A7 전역 보정 렌더링** — `app/lib/features/editor/rendering/photo_filter.dart` | ✅ 슬라이더 6종이 이미지에 실제 반영, 길게 눌러 원본 비교. 단위 13 + 육안 프리뷰 테스트 |
> | 2 | **B3 편집 권한 서버 강제** — `ws.py: _authorize_edit` | ✅ 얼굴 클레임/공용영역 정책/방 잠금/경로 검증 |
> | 2 | **B5 개인별 undo/redo** — `state.py` member_id별 스택 | ✅ |
> | 2 | **B6 기능별 리셋** — `reset_param` | ✅ |
> | 2 | **B8 다중 업로드** — `POST /sessions`(N장), `POST /sessions/{id}/photos` | ✅ |
> | 2 | **B9 6명 / 24시간** | ✅ |
> | 2 | **B4 공용영역 권한 설정** — `POST /sessions/{id}/global-edit-policy` | ✅ (B3 구현에 필요해 함께 처리) |
> | 2 | **실시간 커서 좌표** — `presence.cursor {x,y}` | ✅ 백엔드만. 앱 UI는 4단계 |
> | 3 | **B7 완료 확정** — `PhotoCompletion` + `complete`/`finalized` | ⬜ 다음 |
> | 4 | **얼굴 클레임 UI + 타인영역 잠금 표시 + 커서 렌더링** | ⬜ |
>
> 백엔드 테스트: `backend/tests/` — 45개 통과 (`pytest`)
>
> ⚠️ 미해결
> - 실기기 E2E 미검증 — Android SDK 미설치. 웹 빌드 또는 Android Studio 필요
> - `app/pubspec.yaml`의 Dart SDK 제약을 `^3.12.2` → `^3.11.0`으로 낮춘 상태 (로컬 Flutter 3.41.9 기준). 팀 간 Flutter 버전 합의 필요
> - `datetime.utcnow()` deprecation 경고 (Python 3.14) — 동작엔 지장 없으나 추후 정리 필요

> 앱 진행 현황 (2026-07-18):
> - ✅ 도메인 모델(`room/domain/session_models.dart`) — 백엔드 SessionDetail/Photo/Face/EditState JSON 파싱
> - ✅ REST 저장소(`room/data/room_repository.dart`) — join / getSession + 방별 member 토큰 store
> - ✅ 방 컨트롤러(`room/application/room_controller.dart`) — RoomSocket 연결, `state_sync`/`edit_applied`/`presence_update`/락/클레임/강퇴 수신, 낙관적 편집 전송, JWT에서 내 memberId 추출
> - ✅ 에디터 화면 — 네트워크 이미지 + 전역 슬라이더 6종(밝기/대비/채도/색온도/하이라이트/그림자) 실시간 반영·전송, 참여자 프레즌스 라벨, undo/redo, 소프트락 표시
> - ✅ 방 화면 — 세션 로드, 참여자 아바타(고유 색상·접속 표시), 초대 코드/링크 복사, 컷 선택 → 에디터 진입
> - ⏳ 남은 백엔드 의존: 완료 확정(B7 `complete`/`finalized`), 편집 권한 서버 강제(B3), 개인별 undo(B5). 프론트는 프로토콜 훅(`ws_client.dart`)만 두고 대기.
> - ⏳ 남은 프론트: 얼굴 클레임 UI(탭→"이게 나예요"), 파라미터→이미지 실제 렌더링(A7, 현재는 원본만 표시), 방 생성 플로우(홈 FAB → `POST /sessions`, 로그인/토큰 필요 → M2)

**M2 — 회원·홈·초대**
B1·B2·B12 + A1·A2·A9·A10. 가입 → 홈 → 방 생성 → 링크 초대 전체 플로우.

**M3 — 앨범 (비동기 협업)**
B10·B11 + A4·A8. 앨범 생성/업로드/푸시/할 일/자동 확정/방장 마감.

**M4 — 보정 고도화 + 마무리**
얼굴 워핑·피부 보정(A7 확장), 인앱 카메라(A3), 실제 소셜 OAuth(B13), 방 만료→앨범 이관, 운영 전환(PostgreSQL/S3/Redis 검토).

## 4. 2인 분업 제안

- **백엔드 담당(친구)**: B1~B13 — v0.1을 만든 사람이 이어서 확장하는 게 가장 빠름.
- **앱 담당**: A1~A10 — `app/` 폴더. WS 프로토콜(`docs/아키텍처.md`)이 인터페이스 계약.
- 매 마일스톤 끝에서 함께 E2E 테스트. API 변경은 반드시 `docs/아키텍처.md`에 먼저 반영 후 구현.
